public class test {
    int i,j,n;
                for (i = 0; i < n ; i++) {
                    for (j =0; j < n; j++) {
                        System.out.println(i +" " +j+ " "+n);
                        /* if (empid == id) {
                            workhours = worktime_calculator.wcalc(einfo[4], einfo[5]);
                            workweek[i][j] = { work_date,toString(workhours) };
                        } */
                    } 
               }
    
}
